# Signaling server for development builds.
# Points to Render so that two machines on npm run dev can reach the same room.
# For offline / localhost-only testing: change to http://127.0.0.1:3001
VITE_SIGNALING_URL=https://kgb-sound-system-1984.onrender.com
